// pegando os inputs do usuário
up		=keyboard_check(vk_up);
down	=keyboard_check(vk_down); 
left	=keyboard_check(vk_left);
right	=keyboard_check(vk_right);
attack = keyboard_check_pressed(vk_escape);


// Aplicando os inputs á velocidade 
// resultdo da conta vai ser 0, -1 ou 1 



//Ajustando a diração com base na direção que ele está indo 



// Rodando a minha maquina de estados 

roda_estado();